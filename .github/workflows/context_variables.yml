name: Context Variables

on:
  push:
    brnches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test_context_variables:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "Testing Context Variables"
  
        run: echo "Repository Name is ${{ github.reposiory }}"
          

