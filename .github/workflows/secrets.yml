name: Secrets 

on:
  #push:
    #branches: [ "main" ]
  #pull_request:
    #branches: [ "main" ]
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true

jobs:
  test_context_variables:
    runs-on: ubuntu-latest
    steps:
      - name: Checking Secret Variable
        run: echo "Secret Value is ${{ secrets.DB_PASSWORD }}"
      
      #- name: "Environment Variables"
      #  run: echo "$ACTIONS_STEP_DEBUG"

